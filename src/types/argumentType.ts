/**
 * Possible type of argument
 * @type {enum}
 */
export enum ArgumentType {
    string = 'string',
    number = 'number',
    boolean = 'boolean',
    object = 'object',
    array = 'array',
    undefined = 'undefined',
}
